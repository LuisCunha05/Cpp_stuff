#include <string>
#include <iostream>
#include<map>
using namespace std;

int main(){
    string first;
    getline(cin, first);

    string second;
    getline(cin, second);

    map<string, int> word;
    int last_index = 0, index = 0;
    word.

    cout << first << "\n";
}